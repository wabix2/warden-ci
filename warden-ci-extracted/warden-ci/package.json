{
  "name": "warden-ci",
  "version": "0.2.0",
  "description": "Warden CI \u2014 GitHub App that scans pull requests for AI-generated, unverifiable, or tampered code and flags risky dependencies.",
  "main": "lib/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node lib/server.js",
    "dev": "npm run build && npm run start",
    "test": "npm run build && node --test lib/heuristics.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "probot": "^13.3.0",
    "@google/generative-ai": "^0.21.0",
    "@babel/parser": "^7.24.0",
    "@babel/traverse": "^7.24.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7",
    "@types/babel__traverse": "^7.20.5"
  },
  "engines": {
    "node": ">=18"
  }
}
